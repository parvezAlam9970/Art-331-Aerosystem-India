import React from 'react'
import CustomModalWrapper from '../CustomModalWrapper'

const RangeMapModal = ({type}) => {
  return (
    <CustomModalWrapper type={type}>
       <div className=' bg-green-300 h-[100px] uppercase'>
         tesgin one modal
       </div>
    </CustomModalWrapper>
  )
}

export default RangeMapModal